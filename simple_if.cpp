#include <iostream>
using namespace std;

int main()
{
    int number;

    // Taking input from user
    cout << "Enter a number: ";
    cin >> number;

    // Using simple if statement
    if (number > 0)
    {
        cout << "The number is positive." << endl;
    }

    return 0;
}